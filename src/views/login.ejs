<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #04100c 0%, #0a1f15 50%, #04100c 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }
        
        body::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 50% 50%, rgba(0, 170, 85, 0.1) 0%, transparent 50%);
            animation: pulse 15s ease-in-out infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }
        
        .login-container {
            background: rgba(10, 21, 16, 0.95);
            backdrop-filter: blur(10px);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 170, 85, 0.2);
            border: 1px solid rgba(0, 170, 85, 0.3);
            text-align: center;
            max-width: 450px;
            width: 100%;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }
        
        .login-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #0a5, #4fd89b, #0a5);
            background-size: 200% 100%;
            animation: shimmer 3s linear infinite;
        }
        
        @keyframes shimmer {
            0% { background-position: -200% 0; }
            100% { background-position: 200% 0; }
        }
        
        .logo {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #0a5, #4fd89b);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 30px rgba(0, 170, 85, 0.3);
        }
        
        .subtitle {
            color: #4fd89b;
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
        }
        
        .author {
            color: #6b8f7b;
            font-size: 0.9rem;
            margin-bottom: 2rem;
        }
        
        .welcome-text {
            color: #e0e6ed;
            margin-bottom: 2.5rem;
            line-height: 1.6;
        }
        
        .google-btn {
            display: inline-flex;
            align-items: center;
            background: linear-gradient(135deg, #0a5, #00cc66);
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 10px;
            text-decoration: none;
            font-size: 16px;
            font-weight: 500;
            transition: all 0.3s ease;
            gap: 12px;
            box-shadow: 0 8px 25px rgba(0, 170, 85, 0.4);
            position: relative;
            overflow: hidden;
        }
        
        .google-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .google-btn:hover::before {
            left: 100%;
        }
        
        .google-btn:hover {
            background: linear-gradient(135deg, #00cc66, #0a5);
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(0, 170, 85, 0.5);
        }
        
        .google-icon {
            width: 22px;
            height: 22px;
            background: white;
            border-radius: 3px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .error-message {
            background: rgba(220, 38, 38, 0.1);
            color: #ff6b6b;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 1rem;
            border-left: 4px solid #ff6b6b;
            border: 1px solid rgba(220, 38, 38, 0.3);
        }
        
        .success-message {
            background: rgba(0, 170, 85, 0.1);
            color: #4fd89b;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 1rem;
            border-left: 4px solid #4fd89b;
            border: 1px solid rgba(0, 170, 85, 0.3);
        }
        
        .footer {
            margin-top: 2rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(0, 170, 85, 0.2);
            color: #6b8f7b;
            font-size: 0.85rem;
        }
        
        .current-time {
            color: #4fd89b;
            font-size: 0.8rem;
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="logo">Codeplat</div>
        <div class="subtitle">Code Editor</div>
        <div class="author">by nitinrajput16</div>
        
        <% if (error === 'auth_required') { %>
            <div class="error-message">
                🔒 You need to login to access this page
            </div>
        <% } %>
        
        <% if (message === 'logged_out') { %>
            <div class="success-message">
                ✅ You have been successfully logged out
            </div>
        <% } %>
        
        <p class="welcome-text">
            Welcome to Codeplat, a powerful online code editor.<br>
            Please sign in with your Google account to start coding!
        </p>
        
        <% const nextQuery = next ? ('?next=' + encodeURIComponent(next)) : ''; %>
        <a href="/auth/google<%= nextQuery %>" class="google-btn">
            <div class="google-icon">
                <svg width="18" height="18" viewBox="0 0 24 24">
                    <path fill="#4285f4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                    <path fill="#34a853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                    <path fill="#fbbc05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                    <path fill="#ea4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                </svg>
            </div>
            Sign in with Google
        </a>
        
        <div class="footer">
            <p>Secure authentication powered by Google OAuth 2.0</p>
            <div class="current-time">
                <%= new Date().toLocaleString('en-IN', { timeZone: 'Asia/Kolkata' }) %> IST
            </div>
        </div>
    </div>
</body>
</html>